{
  "name": "@brainbridge/bridge",
  "version": "0.1.0",
  "description": "Brain Bridge relay server (Phase 2.1+ - reserved for WebSocket relay)",
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
